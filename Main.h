/*#pragma once
#include <opencv2/videoio.hpp>;
#include <opencv2/opencv.hpp>;
#include <iostream>;
#include "PoseEstimation.h";

using namespace cv;
using namespace std;

#define EX5 1
#define EX5_RAW 0
#define DRAW_CONTOUR 0
#define DRAW_RECTANGLE 0
#define LOG_ID 1

#define THICKNESS_VALUE 4

// Struct holding all infos about each strip, e.g. length
struct MyStrip {
	int stripeLength;
	int nStop;
	int nStart;
	Point2f stripeVecX;
	Point2f stripeVecY;
};*/

//static void on_trackbar(int, void*);
//void bw_trackbarHandler(int, void*);
//int subpixSampleSafe(const Mat&, const Point2f&);
//Mat calculate_Stripe(double, double, MyStrip&);
//int main();